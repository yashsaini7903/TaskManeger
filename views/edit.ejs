<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update Task</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center p-6">
  
  <div class="w-full max-w-lg bg-zinc-950 border border-zinc-800 rounded-xl shadow-lg p-8">
    <!-- Heading -->
    <h1 class="text-2xl font-bold text-yellow-400 mb-6"> Update Task</h1>
    
    <!-- Form -->
    <form action="/edit/<%= task._id %>" method="post" class="space-y-5">
      
      <!-- Task Title -->
      <div>
        <label for="title" class="block text-sm font-medium text-zinc-300 mb-1">Task Title</label>
        <input type="text" id="title" name="title" value="<%= task.title %>" 
               placeholder="Enter task title"
               class="w-full px-4 py-3 rounded-lg bg-zinc-900 border border-zinc-700 text-white focus:outline-none focus:ring-2 focus:ring-yellow-400">
      </div>
      
      <!-- Notes -->
      <div>
        <label for="notes" class="block text-sm font-medium text-zinc-300 mb-1">Notes</label>
        <textarea id="notes" name="notes" rows="4"
                  placeholder="What’s on your mind?"
                  class="w-full px-4 py-3 rounded-lg bg-zinc-900 border border-zinc-700 text-white resize-none focus:outline-none focus:ring-2 focus:ring-yellow-400"><%= task.notes %></textarea>
      </div>
      
      <!-- Status -->
      <div>
        <label for="status" class="block text-sm font-medium text-zinc-300 mb-1">Status</label>
        <select id="status" name="status"
                class="w-full px-4 py-3 rounded-lg bg-zinc-900 border border-zinc-700 text-white focus:outline-none focus:ring-2 focus:ring-yellow-400">
          <option value="pending" <%= task.status === 'pending' ? 'selected' : '' %>>Pending</option>
          <option value="completed" <%= task.status === 'completed' ? 'selected' : '' %>>Completed</option>
        </select>
      </div>
      
      <!-- Buttons -->
      <div class="flex items-center justify-between">
        <a href="/" class="px-4 py-2 bg-zinc-700 hover:bg-zinc-600 rounded-lg text-sm font-medium transition">
          Cancel
        </a>
        <button type="submit" 
                class="px-6 py-2 bg-yellow-500 hover:bg-yellow-600 text-black font-semibold rounded-lg shadow-md transition">
          Update Task
        </button>
      </div>
    </form>
  </div>
  
</body>
</html>
